//* Para a utilização do createContext é necessário a importação do useContext. É uma funcionalidade que permite que você tenha acesso aos estados e funções de contexto. É uma biblioteca nativa do React.
import { createContext } from "react";

export const GlobalStateContext = createContext();
